The following function should take a list of integers as input and returns the average (arithmetic mean) of those integers. If the list is empty, the function should return None. Is this correctly implemented?